generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "postgresql"
  url          = env("DATABASE_URL")
}

model tb_metrics {
  id Int @id @default(autoincrement())
  data DateTime 
  temperatura Int 
  eficiencia Int
}